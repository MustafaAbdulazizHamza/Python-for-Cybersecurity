import os
import subprocess

os.chdir()
os.rename()
os.mkdir()
os.path.exists("path") # Return True or False
os.path.isfile("file")
os.path.isdir("dir") # 

subprocess.run("cd folder", shell=True)
# python-nmap --> Subprocess
# XML --> parsing --> Dictionary
# OS executes the command in the current process
# Subprocess executes the command in a subprocess